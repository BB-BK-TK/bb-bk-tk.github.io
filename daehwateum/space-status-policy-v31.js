(function(){'use strict';
var ko=(navigator.language||'ko').toLowerCase().indexOf('ko')===0,patching=false,syncing=false,syncPromise=null,lastSync=0,scheduled=false;
var SUPA='https://kacvynoegfpvgdpqtjdi.supabase.co';
function esc(v){return window.DT&&DT.esc?DT.esc(v):String(v==null?'':v)}
function copy(){return ko?{active:'내 대화틈',waiting:'초대 대기 중',waitingBody:'상대방이 들어오면 대화가 시작돼요.',waitingState:'아직 시작 전 · 상대방 참여 대기',invite:'초대하기',newSpace:'새로운 대화틈 만들기'}:{active:'My spaces',waiting:'Waiting for someone to join',waitingBody:'The conversation starts when someone joins your invitation.',waitingState:'Not started · Waiting for someone to join',invite:'Invite',newSpace:'Create another space'}}
function pending(s){var names=Array.isArray(s&&s.memberNames)?s.memberNames.filter(Boolean):[];if(names.length)return names.length<2;return !(s&&s.partnerName)}
function signature(list){return list.map(function(s){var n=Array.isArray(s.memberNames)?s.memberNames.filter(Boolean).length:(s.partnerName?2:1);return s.room+':'+n+':'+(s.lastRound||1)+':'+(s.freeDay||'')+':'+(s.freePeriodEnded?'1':'0')+':'+(s.homeAnswerStatus||'')+':'+(s.homeAnswerArrived?'1':'0')}).join('|')}
function hasAnswered(p){return !!(p&&(p.answered===true||(typeof p.answer==='string'&&p.answer.trim().length>0)))}
function isRevealed(s,x){var rev=Array.isArray(s&&s.rev)?s.rev:[];return !!(x&&x.round_id&&rev.indexOf(x.round_id)>=0)}
function answerStatus(x,s){
  if(!x||Number(x.participant_count||0)<2)return{label:null,arrived:false};
  if(x.can_start_next)return{label:ko?'새 질문 도착':'New question ready',arrived:true};
  var me=x.me||null;if(me&&!hasAnswered(me))return{label:ko?'새 질문 도착':'New question ready',arrived:true};
  var myName=me&&me.name?me.name:'',parts=Array.isArray(x.participants)?x.participants:[],others=parts.filter(function(p){return !p.is_me&&(!myName||p.name!==myName)});if(!others.length&&x.partner&&x.partner.name)others=[x.partner];if(!others.length)return{label:null,arrived:false};
  if(!x.unlocked){var wait=others.filter(function(p){return !hasAnswered(p)});if(wait.length){if(others.length===1){var name=others[0].name||(ko?'상대방':'Partner');return{label:ko?name+'님의 답을 기다리는 중':'Waiting for '+name+' to answer',arrived:false}}return{label:ko?'다른 사람의 답을 기다리는 중':'Waiting for others to answer',arrived:false}}}
  if(x.unlocked&&!isRevealed(s,x))return{label:ko?'답변이 모두 도착했어요':'All answers are ready',arrived:true};
  if(x.is_paused)return{label:ko?'대화가 잠시 쉬고 있어요':'Conversation paused',arrived:false};
  if(x.unlocked)return{label:ko?'다음 질문을 기다리는 중':'Waiting for the next question',arrived:false};
  return{label:null,arrived:false}
}
function normalizeLegacy(s){var legacy=s&&(s.homeAnswerStatus==='7일 대화가 완료됐어요'||s.homeAnswerStatus==='Your 7-day conversation is complete');if(!legacy)return false;s.homeAnswerStatus=null;s.homeAnswerArrived=false;return true}
function persist(list){try{var r=JSON.parse(localStorage.getItem('dt.spaces.v1')||'{}');if(!r||!Array.isArray(r.spaces))return;r.spaces=r.spaces.map(function(old){var fresh=list.filter(function(x){return x.room===old.room})[0];return fresh?Object.assign({},old,fresh):old});localStorage.setItem('dt.spaces.v1',JSON.stringify(r))}catch(e){}}
function fetchState(s){if(!s||!s.room||!s.token)return Promise.resolve(null);return fetch(SUPA+'/rest/v1/rpc/dt_get_state',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({p_room_id:s.room,p_participant_token:s.token})}).then(function(r){return r.ok?r.json():null}).catch(function(){return null})}
function syncStatuses(force){if(!window.DT||!DT.spaces)return Promise.resolve(false);var now=Date.now();if(!force&&now-lastSync<15000)return Promise.resolve(false);if(syncing&&syncPromise)return syncPromise;var list=DT.spaces();if(!list.length)return Promise.resolve(false);syncing=true;lastSync=now;syncPromise=Promise.all(list.map(fetchState)).then(function(states){var changed=false;states.forEach(function(x,i){if(!x)return;var s=list[i],names=(x.participants||[]).map(function(p){return p.name}).filter(Boolean),before=s.homeAnswerStatus||'',status=answerStatus(x,s);s.memberNames=names;s.partnerName=x.partner&&x.partner.name?x.partner.name:s.partnerName;s.lastRound=x.round_sequence||s.lastRound;s.targetParticipants=x.max_participants||s.targetParticipants;s.isPremium=!!x.is_premium;s.freeDay=x.free_day||s.freeDay;s.freePeriodEnded=!!x.free_period_ended;s.isPaused=!!x.is_paused;s.lastSeen=new Date().toISOString();s.homeAnswerStatus=status.label;s.homeAnswerArrived=status.arrived;s.homeAnswerRound=x.round_sequence||null;if(before!==(s.homeAnswerStatus||''))changed=true});persist(list);return changed}).catch(function(){return false}).then(function(v){syncing=false;syncPromise=null;return v},function(){syncing=false;syncPromise=null;return false});return syncPromise}
function paintAnswer(card,s){if(!card)return;normalizeLegacy(s);var old=card.querySelector('.space-answer-status');if(old)old.remove();var day=card.querySelector('div>p');if(day){if(s.freePeriodEnded)day.textContent=s.isPremium?'ROUND '+(s.lastRound||1):(ko?'7일 완료':'7 DAYS');else day.textContent='DAY '+(s.freeDay||1)}if(!s.homeAnswerStatus||pending(s))return;var el=document.createElement('p');el.className='space-answer-status '+(s.homeAnswerArrived?'arrived':'waiting');el.innerHTML='<span class="space-status-dot"></span>'+esc(s.homeAnswerStatus);var box=card.querySelector(':scope > div');if(box)box.appendChild(el)}
function patch(){if(patching||!window.DT||!DT.spaces)return;var root=document.querySelector('.spaces');if(!root||root.getAttribute('data-status-ready')!=='1')return;var list=DT.spaces().slice().reverse(),sig=signature(list);if(root.getAttribute('data-space-status-v31')===sig)return;patching=true;requestAnimationFrame(function(){if(!document.documentElement.contains(root)){patching=false;return}var c=copy(),cards={};root.querySelectorAll('.spacecard').forEach(function(card){var b=card.querySelector('[data-room]');if(b)cards[b.getAttribute('data-room')]=card});var active=[],waiting=[];list.forEach(function(s){var card=cards[s.room];if(!card)return;(pending(s)?waiting:active).push({space:s,card:card})});root.innerHTML='';root.setAttribute('data-space-status-v31',sig);if(active.length){var h=document.createElement('h2');h.textContent=c.active;root.appendChild(h);active.forEach(function(x){x.card.classList.remove('pending-spacecard');paintAnswer(x.card,x.space);root.appendChild(x.card)})}if(waiting.length){var group=document.createElement('section');group.className='spaces-awaiting';group.innerHTML='<div class="spaces-awaiting-head"><h2>'+esc(c.waiting)+'</h2><p>'+esc(c.waitingBody)+'</p></div>';waiting.forEach(function(x){var card=x.card;card.classList.add('pending-spacecard');var old=card.querySelector('.space-answer-status');if(old)old.remove();var p=card.querySelector('div>p');if(p)p.textContent=c.waitingState;var btn=card.querySelector('[data-a="open-room"]');if(btn)btn.textContent=c.invite;group.appendChild(card)});root.appendChild(group)}var footer=document.createElement('p');footer.className='c';footer.innerHTML='<button class="text" data-a="new">'+esc(c.newSpace)+'</button>';root.appendChild(footer);patching=false})}
function prime(){scheduled=false;var root=document.querySelector('.spaces');if(!root||!window.DT||!DT.spaces)return;var list=DT.spaces();if(root.getAttribute('data-status-ready')!=='1')root.setAttribute('data-status-ready','1');patch();if(root.getAttribute('data-status-loading-v31')==='1')return;root.setAttribute('data-status-loading-v31','1');syncStatuses(true).then(function(changed){if(!document.documentElement.contains(root))return;root.removeAttribute('data-status-loading-v31');if(changed){root.removeAttribute('data-space-status-v31');patch()}})}
function schedule(){if(scheduled)return;scheduled=true;setTimeout(prime,0)}
function start(){var app=document.getElementById('app');if(app)new MutationObserver(schedule).observe(app,{childList:true,subtree:true});schedule();setInterval(function(){if(!document.hidden&&document.querySelector('.spaces')){lastSync=0;prime()}},15000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();document.addEventListener('visibilitychange',function(){if(!document.hidden){lastSync=0;schedule()}});
})();